name: tcn
type: dl_supervised

architecture:
  input_features: 60
  window_size: 32
  channels: [64, 64, 128, 128]
  kernel_size: 3
  dropout: 0.2
  head:
    hidden_dims: [128, 64]
    dropout: 0.3
  output:
    num_classes: 2
    activation: sigmoid
